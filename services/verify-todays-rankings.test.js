import verifyTodaysRankings from './verify-todays-rankings';

describe('verifyTodaysRankings', () => {
  it('should exist', () => {
    expect(typeof verifyTodaysRankings).toEqual('function');
  });
});

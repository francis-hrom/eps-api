import addNewTargetsFromGsheet from '../add-new-targets-from-gsheet';

describe('addNewTargetsFromGsheet', () => {
  it('should exist', () => {
    expect(typeof addNewTargetsFromGsheet).toEqual('function');
  });
});
